'use strict';
/*
const sqliteDriver = require('../utils/sqliteDriver');

// const db = sqliteDriver.initialize( options.serial, logfile);

const dbOptions = {
    dbPath: `${process.env.HOME}/tf.db`,
    templateDB: `${__dirname}/../template/template.db`
};


module.exports = {
    dbOptions
};
*/
